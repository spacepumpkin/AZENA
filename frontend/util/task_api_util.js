/* Task API Util Functions:
  - fetchTask(taskId)
  - createTask(task)
  - updateTask(task)
  - deleteTask(taskId)

  - fetchTasks(userId) // fetch all tasks associated with user
*/

// export const createTask = (task) => {
//   console.log(`creating new task...`);
//   return $.ajax({
//     url: `/api/tasks`,
//     method: "POST",
//     data: { task: {  } }
//   })
// }